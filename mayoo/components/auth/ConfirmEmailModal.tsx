import React from 'react'
import { Modal, View } from 'react-native'

export default function ConfirmEmailModal({showConfirmEmailModal}) {
  return (
    <Modal animationType="slide"
    transparent={false}
    visible={modifyModalVisible}>></Modal>
  )
}
